{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env PORT=5173 BASE_PATH=/ npm run dev --workspace @workspace/nivi-chat",
    "dev:chat": "cross-env PORT=5173 BASE_PATH=/ npm run dev --workspace @workspace/nivi-chat",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspaces --if-present"
  },
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  },
  "packageManager": "npm@10.9.2"
}
